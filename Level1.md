## 🎯 Level Goal: Find the password for the next level

Now that you’re inside the Bandit server, the goal is to read the contents of a file called `readme`.  
This file holds the password you’ll need to move on.

---

### 📂 Where’s the file?
When you log in, you’re placed in your **home directory**. The file `readme` is sitting right there, waiting for you.
<br>Try commands like `ls`,`cat`. to loacte and open the file.

---
The `ls` command is used for listing the content in the directry.
- Basic Syntax:
  ```bash
    ls
---
The `cat` Command is short for **concatenate**.  
Its main job is to display the contents of a file directly in your terminal.

- Basic Syntax:  
  ```bash
  cat <filename>
<br>`I will explain more about file System and commands in next level.`
 
  <br>**🚀 Steps to Solve**
- Log in with SSH (from Level 0).
- Run:
`cat readme`
- Copy the password that appears.
- Use it to log into the next level.

`Congratulation on solving level1 !!!`
